name: echo

entryPoint: main.sc

nlp:
  tokenizer: myStem
  vocabulary: common-vocabulary.json
  costStrategy: weighted

classifier:
  enabled: true
  engine: sts
  noMatchThreshold: 0.2
  parameters:
    algorithm: bow

tests:
  exclude:
    - test.xml
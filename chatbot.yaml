name: classifier-template

entryPoint:
  - main.sc

botEngine: v2
language: ru

tests:
  include:
    - test.xml

dependencies:
  - name: common
    type: git
    url: https://bitbucket.org/just-ai/zb-common
    version: heads/master

sts:
  noMatchThreshold: 0.2

caila:
  noMatchThreshold: 0.2
